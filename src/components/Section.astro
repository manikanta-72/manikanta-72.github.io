---
interface Props {
  id: string;
  title: string;
  class?: string;
}
const { id, title, class: cls = '' } = Astro.props;
---

<section id={id} class={cls}>
  <div class="wrap">
    <h2 class="heading">{title}</h2>
    <slot />
  </div>
</section>
